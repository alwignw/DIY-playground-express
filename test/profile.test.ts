describe('GET profile/wellness', () => {
  it('should get profile wellness', async () => {
    expect(true).toBe(true)
  })
  // it('reject if header auth not defined', async () => {})
  // it('reject if invalid token (not contain bearer)', async () => {})
  // it('reject if profile not found', async () => {})
  // it('reject if data company not found', async () => {})
  // it('reject if data polis not found', async () => {})
  // it('reject if data claim not found', async () => {})
  // it('reject if data point not found', async () => {})
  // it('reject if data walk not found', async () => {})
})